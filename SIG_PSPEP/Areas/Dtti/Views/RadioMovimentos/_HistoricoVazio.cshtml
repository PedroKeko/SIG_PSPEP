<div class="d-flex flex-column align-items-center justify-content-center p-5">
    <div class="empty-state-icon mb-3">
        <i class="fas fa-history fa-3x text-warning"></i>
    </div>
    <h5 class="text-secondary mb-2 animate-fade-in">Nenhum histórico encontrado</h5>
    <p class="text-muted text-center animate-fade-in" style="max-width: 320px;">
        Este rádio ainda não possui registros de movimentação.
        Assim que forem adicionados, eles aparecerão aqui automaticamente.
    </p>
</div>

<style>
    /* Ícone com leve pulsação */
    .empty-state-icon i {
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0%

    {
        transform: scale(1);
        opacity: 1;
    }

    50% {
        transform: scale(1.2);
        opacity: 0.7;
    }

    100% {
        transform: scale(1);
        opacity: 1;
    }

    }

    /* Fade-in suave no texto */
    .animate-fade-in {
        opacity: 0;
        animation: fadeIn 0.8s ease-in forwards;
    }

    @@keyframes fadeIn {
        from

    {
        opacity: 0;
        transform: translateY(10px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }
</style>

