@model SIG_PSPEP.Entidades.LogsAcesso

<div class="modal-header bg-primary text-white">
    <h5 class="modal-title">
        <i class="bi bi-info-circle-fill me-2"></i> Detalhes do Log de Acesso
    </h5>
    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
</div>

<div class="modal-body">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex justify-content-between">
                    <strong>Tipo de Acesso:</strong>
                    <span class="badge bg-info text-dark">@Model.TipoAcesso</span>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                    <strong>Observações:</strong>
                    <span>@Model.Obs</span>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                    <strong>Data de Registo:</strong>
                    <span>@Model.DataRegisto.ToString("dd/MM/yyyy HH:mm")</span>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                    <strong>Usuário:</strong>
                    <span class="text-muted">@Model.User?.UserName</span>
                </li>
            </ul>
        </div>
    </div>
</div>
